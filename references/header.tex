% Define chapter counter if document class doesn't have one (e.g. article)
\ifdefined\thechapter\else
  \newcounter{chapter}
  \renewcommand*{\thechapter}{\arabic{chapter}}
\fi

\usepackage{setspace}
\onehalfspacing

\usepackage{lscape}

% kableExtra loads pdflscape which sets /Rotate 90 on PDF pages, making them appear
% horizontal in the viewer. Reset to /Rotate 0 just before each page ships so pages
% stay portrait while table content remains rotated 90° by lscape on the page.
\AddToHook{shipout/before}{\pdfpageattr{/Rotate 0}}

\usepackage{xcolor}
\usepackage{tikz}
\usepackage{afterpage}
\usepackage{eso-pic}
\definecolor{thesiscream}{RGB}{255, 252, 240}
\definecolor{thesissand}{RGB}{232, 212, 162}
\pagecolor{thesiscream}
